package stringsx

// //返回第一次出现sep之后的字符串
// func (s *Stringx) After(sep string) string {
// 	s.str = After(s.str, sep)
// 	return s.str
// }

// //返回最后一次出现sep之后的字符串
// func (s *Stringx) AfterLast(sep string) string {
// 	s.str = AfterLast(s.str, sep)
// 	return s.str
// }
